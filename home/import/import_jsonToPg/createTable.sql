CREATE TABLE test (
    key text PRIMARY KEY,   
    summary text,
    priority TEXT,
    resolution TEXT,
    status TEXT,
    assignee TEXT,
    reporter TEXT,
    createdDate TEXT,
    updatedDate TEXT,
    resolutionDate TEXT,
    fixVersions TEXT,
    affectedVersions text,
    labels text
);
